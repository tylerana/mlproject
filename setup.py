from setuptools import find_packages, setup

setup(
    name='mlproject',
    version='0.0.1',
    author='Tyler',
    author_email='tanatol000@citymail.cuny.edu',
    packages=find_packages(), 
    install_requires=['pandas', 'numpy', 'seaborn']
)
